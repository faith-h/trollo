<h1><%= @board.id ? 'Edit Board' : 'New Board' %></h1>
<hr />
<%= form_with(model: board, local: true, class: "ui form") do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "New Board Name", autofocus: true; %>
  </div>
<% end %>