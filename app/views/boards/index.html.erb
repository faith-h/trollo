<h1 class="ui header">Your Boards</h1>
<% @boards.each do |board| %>
  <div class="valign-wrapper">
  <div class="card-panel hoverable">
        <h1><%= link_to board.name, board_path(board) %></h1>
<% end %>
  </div>
  </div>
  <br />
